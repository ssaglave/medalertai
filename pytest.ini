[pytest]
testpaths = tests
markers =
    slow: integration or artifact-backed tests that may be skipped or take longer to run
addopts = --strict-markers -v

[tool:pytest]
filterwarnings =
    ignore::sklearn.exceptions.InconsistentVersionWarning
